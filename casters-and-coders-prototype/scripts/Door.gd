extends StaticBody2D

export (String) var door_to = "room_name"
export (String) var object_type = "object_type"

# Called when the node enters the scene tree for the first time.
func _ready():
	pass # Replace with function body.
